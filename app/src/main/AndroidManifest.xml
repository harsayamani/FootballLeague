<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobile.harsoft.clubsdefootball">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_soccer"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_soccer"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".DetailPrevMatchActivity"
            android:theme="@style/AppTheme"
            android:label="Match Detail"/>
        <activity android:name=".DetailNextMatchActivity"
            android:theme="@style/AppTheme"
            android:label="Match Detail"/>
        <activity android:name=".MatchScheduleActivity"
            android:parentActivityName=".MainActivity"
            android:label="Match Schedule"/>
        <activity
            android:name=".DetailLeagueActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>